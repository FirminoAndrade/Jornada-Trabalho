export interface IDiaria {
  id?: string,
  atividade: string,
  numero_veiculo: string,
  linha: number,
  km_inicial: number,
  km_final: number,
  hora_inicial: string,
  hora_final: string,
  data: string,
  valor_depositado: string,
  numero_passageiro: number,
  observacao: string
}

